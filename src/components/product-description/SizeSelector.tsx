import React from "react";

const SizeSelector: React.FC = () => (
    <p className="mt-4 text-lg">
        <strong>Select Your Size:</strong> Choose between the 90x60 cm and 50x30 cm sizes to suit your space and style
        preferences.
    </p>
);

export default SizeSelector;
